<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ingredient">
	<select id="ingredient" resultType="Ingredient">
		SELECT * FROM ingredients
		WHERE recipeid=#{recipeid}
	</select>
	
	<insert id="addmore" useGeneratedKeys="true">
		INSERT INTO ingredients (recipeid, ingname, quantity, unit, exp)
		VALUES (#{recipeid}, #{ingname}, #{quantity}, #{unit}, #{exp})
	</insert>
	
	<update id="updatemore">
		UPDATE ingredients (recipeid, ingname, quantity, unit, exp)
		SET recipeid=#{recipeid}, ingname=#{ingname}, quantity=#{quantity},unit=#{unit}, exp=#{exp}}
		WHERE recipeid=#{recipeid}
	</update>
	
	<delete id="deletefirst">
		DELETE FROM ingredients
		WHERE recipeid=#{recipeid}
	</delete>
</mapper>