<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mealkit">
	<select id="list" resultType="mealkit">
		SELECT *
		FROM mealkit
	</select>
	
	<insert id="add" useGeneratedKeys="true" keyProperty="kitid">
		INSERT INTO mealkit (kititle, kitintroduce, kitag, recommend, imagepath, userid)
		VALUES (#{kititle}, #{kitintroduce}, #{kitag}, #{recommend}, #{imagepath}, #{userid})
	</insert>
</mapper>